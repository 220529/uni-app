/**
 * Token配置 - 全局开关
 * 只需要在这里修改 true/false 即可控制Token自动刷新
 */

// 是否启用Token自动刷新
export const AUTO_REFRESH_ENABLED = false;

// 提前刷新时间(毫秒) - 可选配置
export const BUFFER_TIME = 5 * 60 * 1000; // 5分钟

// 跳转延迟(毫秒) - 可选配置
export const REDIRECT_DELAY = 3000; // 3秒
